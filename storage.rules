rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // TEMPORARY: Open for debugging deployment issues
      allow read, write: if true;
    }
  }
}
